<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- the theme applied to the application or activity -->
    <style name="BlueTheme"
           parent="@android:style/Theme.Holo.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/MyActionBar</item>

        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/MyActionBar</item>
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar"
           parent="android:Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/dark_blue</item>
        <item name="android:titleTextStyle">@style/BlueTheme.MyActionBar.TitleTextStyle</item>

        <!-- Support library compatibility -->
        <item name="background">@color/dark_blue</item>
        <item name="titleTextStyle">@style/BlueTheme.MyActionBar.TitleTextStyle</item>
    </style>

    <!-- TitleTextStyle for MyActionBar -->
    <style name="BlueTheme.MyActionBar.TitleTextStyle"
           parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/silver</item>
        <item name="android:textSize">15sp</item>
        <item name="android:textStyle">bold</item>

        <!-- Support library compatibility-->
        <item name="textColor">@color/silver</item>
        <item name="textSize">15sp</item>
    </style>
</resources>